# Todo List · Focus Board

一个无需构建工具的 Todo List 页面，使用原生 HTML、CSS 和 JavaScript 编写。

## 当前功能

- 添加任务
- 标记完成 / 恢复任务
- 删除任务
- 按全部、进行中、已完成筛选
- 使用 `localStorage` 在浏览器本地保存任务

## 运行

直接用浏览器打开 `index.html` 即可。

